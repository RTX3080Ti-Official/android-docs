# Files

**Files** is a **depedency** of SAF (Storage Access Framework) with the package name `com.android.documentsui`.

:::warning

SAF will break if this is disabled.

:::

## Functions

It's the default file selector for other apps.