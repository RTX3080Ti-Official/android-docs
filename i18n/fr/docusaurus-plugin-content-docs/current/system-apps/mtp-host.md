# MTP Host

This app allows you to transfer files directly via USB both ways. It's package name is `com.android.mtp`.

:::danger

Disabling this will remove the ability to use MTP (Media Transfer Protocol) which is what allows you to transfer files via USB.

:::