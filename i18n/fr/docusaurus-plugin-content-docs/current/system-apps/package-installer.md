# Package installer

Package installer lets you install [APK files](../device/apk.mdx) directly without using **ADB**. It's package name is `com.android.packageinstaller`

:::danger

Removing this app will make installing apps only possible via **ADB** or another app that bypasses the **Package installer** (only possible using Root).

:::