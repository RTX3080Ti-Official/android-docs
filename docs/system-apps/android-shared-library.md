# Android Shared Library
This app seems to be a **dummy** app, it has the package name ``android.ext.shared``.

## Code
The only function this app is in a class named ``Version``, the function is named ``GetVersionCode`` which returns 1.