# Drives
Drives in Android devices are typically **UFS** or **eMMC**.

## Types
Android devices uses both **emulated** and **portable** storage. On some devices there is also a storage type for SDCards that has the same format as the external storage.

## Partitions
```mdx-code-block
import DocCardList from '@theme/DocCardList';

<DocCardList />
```